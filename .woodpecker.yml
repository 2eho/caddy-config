when:
  - branch: main
    event: [push]

steps:
  deploy:
    image: curlimages/curl:latest
    commands:
      - ls -la
      - curl -s -X POST --data-binary @./Caddyfile http://159.75.153.53:9876/
